# Add category-card placeholder images, rename "stalls", and fix branding

## What we will change

### 1. Rename the section eyebrow
- Change the eyebrow above the category grid from **"Browse the stalls"** to **"Browse categories"**.

### 2. Add full-width placeholder images to each category card
- Insert a full-width placeholder image area at the top of each category card, between the small note/prehead and the category name.
- Keep the existing card grid, hover effect, and typography.
- Use a neutral placeholder color/token so real photography can drop in later without restyling.

### 3. Fix branding: "Red Tail" → "Redtail"
- Audit all user-facing copy and meta tags across the site.
- Replace instances of **"Red Tail Market"** with **"Redtail Market"** where it appears as the market name.
- Keep the header logo text as provided by the existing logo asset.
- Update footer, page titles, descriptions, and body copy accordingly.

### 4. Preserve existing behavior
- Cards remain links to `/shop`.
- Responsive grid stays the same: 1 → 2 → 3 columns.

## Notes
- No backend or Square work; this is a styling/content adjustment.
- We will use semantic design tokens, not hardcoded colors.

